#!/usr/bin/env sh

# Commit message validation disabled - free-form messages allowed
# You can now use any commit message format you prefer

echo "💬 Commit message validation is disabled - using free-form messages"
exit 0
